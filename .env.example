# =============================================================================
# OpenClaw Environment Variables - Test Configuration
# =============================================================================
# Copy this to .env and customize for your test scenario
# Production: Keep .env in .gitignore (never commit secrets/paths)
# =============================================================================

# AGENT DEFAULTS
# =============================================================================
# Primary model for agents
AGENT_MODEL=moonshot/kimi-k2.5

# Fallback model if primary is unavailable
AGENT_MODEL_FALLBACK=anthropic/claude-haiku-4-5

# Workspace location (where MEMORY.md, AGENTS.md live)
AGENT_WORKSPACE=/home/scott/.openclaw/workspace

# Handbook file reference (AGENT-DEFAULTS-HANDBOOK.md)
AGENT_HANDBOOK_FILE=/home/scott/.openclaw/workspace/AGENT-DEFAULTS-HANDBOOK.md

# Rules file reference (AGENTS.md with If...Then routing)
AGENT_RULES_FILE=/home/scott/.openclaw/workspace/AGENTS.md

# Max concurrent agents in main session
AGENT_MAX_CONCURRENT=4

# Max concurrent sub-agents
AGENT_SUBAGENTS_MAX=8

# =============================================================================
# GATEWAY SETTINGS
# =============================================================================
# Port the gateway listens on
GATEWAY_PORT=18789

# Bind mode: loopback (localhost only), lan (network), tailnet, auto
GATEWAY_BIND=loopback

# Gateway mode: local (self-contained), remote (connects to external)
GATEWAY_MODE=local

# =============================================================================
# CHANNEL TOGGLES (for testing enable/disable)
# =============================================================================
WHATSAPP_ENABLED=true
TELEGRAM_ENABLED=true
DISCORD_ENABLED=true
SLACK_ENABLED=true

# =============================================================================
# TESTING FLAGS
# =============================================================================
# Set to true to enable sandbox/testing mode
TESTING_MODE=false

# Max context tokens for testing
AGENT_CONTEXT_TOKENS=200000

# =============================================================================
# NOTES
# =============================================================================
# After editing this file:
#   1. Save the file
#   2. Restart OpenClaw gateway: openclaw gateway restart
#   3. Verify: openclaw gateway config.get | jq '.agents.defaults'
#
# The env vars will be substituted into openclaw.json at startup.
# Check logs for substitution confirmation.
# =============================================================================
